package com.example.pizza.enam;
/**
 * Enum representing different roles in the system.
 * Roles can be assigned to users to define their access levels and permissions.
 */
public enum Roles {
    ADMIN,
    USER
}
